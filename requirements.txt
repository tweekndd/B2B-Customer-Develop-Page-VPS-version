# AI Trade Customer Analyzer V4.5 - 依赖清单
fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
aiofiles==24.1.0
python-multipart==0.0.20
openpyxl==3.1.5
httpx==0.28.1
beautifulsoup4==4.12.3
pytest==8.3.4
# psycopg2-binary==2.9.10  # PostgreSQL 支持（可选，取消注释后安装）
geopy>=2.4.0
firecrawl-py>=4.0.0  # 网站爬虫降级方案，JS 渲染 / 反爬虫兜底
bcrypt==4.1.2        # 用户密码哈希
itsdangerous>=2.1.2  # Session 签名（Starlette SessionMiddleware 依赖）
jinja2>=3.1.4          # 模板渲染（FileSystemLoader）
cryptography>=3.4.8    # 用户 API Key 加密存储（Fernet，Round 3 新增）
python-dotenv>=1.0.0   # .env 环境变量加载
